# GopherOps 开发与教学交接

## 接手先读

1. docs/project-plan.md：总体设计、阶段顺序、当前证据及下一步。
2. docs/architecture.md：架构概要。
3. 当前任务涉及的实现、接口、迁移和测试；先检查 git status，保留用户未提交修改。

文档中的“计划”不代表已完成，代码中的“能编译”不代表功能已验证。出现冲突时检查实际代码和运行证据，向用户说明，再更新进度。

## 如何指导用户

- 用户有约一年 Go 基础，希望边写项目边学习。默认中文、直接讲代码，不反复讲已掌握的基础语法，不设置强制测验。
- 用户问“下一步”时，只给当前阶段的下一个小闭环：在哪个文件、写什么、为什么、怎么验收；不要一次倒出整套工程。
- 用户说“我写好了/你看下”：先只读审查和安全测试，报告具体问题，不默认代写实现。
- 用户说“帮我改/测试你写”：在指定范围实现并验证，不顺手扩展模块。
- 用户自己写核心练习代码；测试、样板代码由助手代写需符合当次授权。不要为了教学刻意不给必要的示例。
- 解释优先使用类型、接口、数据流、所有权、状态机和调用链。图仅在帮助理解时提供；需要 SVG 时创建可预览文件，不把原始 SVG 当图直接贴出。
- 当前项目是集群诊断 Agent，不混用旧知识库/工单规划，不把独立网关的实现算成本仓库功能。

## 实现规则

- 遵循 domain/application/repository/transport 分层；不得在 handler 中堆数据库和 Agent 循环。
- 数据库生成字段回填、数据库错误与领域错误分离；所有 I/O 接收并传播 context。
- 异步任务的 context 不直接继承已结束的提交请求；订阅断线不自动取消任务。
- 密码仅存哈希，模型密钥/数据库凭据不入库、不写文档、不打印；不读取或复述不必要的本机 secrets。
- 测试默认使用 fake 模型/工具；真实模型调用、真实集群操作须在用户授权范围内。
- 不执行生产数据库迁移、清库或集群写操作。独立测试库也要确认准确目标。
- 不自动提交、推送、部署，不覆盖用户正在写的代码。
- 分阶段完成，不为技术栈数量提前引入 Redis、MQ、MCP、RAG。
- 改动后运行相关测试；可执行 go test ./... 和 go vet ./...。清楚区分通过、失败、跳过、未验证。

## Luna 与交接约定

本文件可供 Luna 或其他模型使用，但不会自动切换会话模型，也不保证指定速度档。
用户后续选择 Luna 后，先核对工作区，接着 docs/project-plan.md 的当前阶段继续；不要因换模型而重新搭骨架或重写已有代码。
完成阶段后更新项目进度和必要的决策记录，交接只需说明：改了什么、怎么验证、剩什么、下一步。普通问答不产生额外学习档案。
