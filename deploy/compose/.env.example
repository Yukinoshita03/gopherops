COMPOSE_PROJECT_NAME=gopherops

# Generate separate values with: openssl rand -hex 24
MYSQL_ROOT_PASSWORD=
IDENTITY_DB_PASSWORD=

# Local-only host port for connecting to MySQL from your machine.
MYSQL_HOST_PORT=3307
